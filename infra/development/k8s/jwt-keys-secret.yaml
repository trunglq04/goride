apiVersion: v1
kind: Secret
metadata:
  name: jwt-keys
type: Opaque
# These are development-only keys generated by `make gen-rsa-keys`.
# In production, use proper key management (e.g. Vault, cloud KMS).
# The actual key data is populated by the Tiltfile using `kubectl create secret`.
